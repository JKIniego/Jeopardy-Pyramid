This should act as the bridge beetween Model and the View. 
e.g. 
GameEngine.java - central controller. should manage the game flow, updates the GameState, processes answers
and tells the view what to display
PlayerActionListener.java - Should listen for button clicks and question panels, then calls methods from GameEngine.